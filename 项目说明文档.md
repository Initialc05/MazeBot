# MazeBot 迷宫导航机器人项目说明文档

## 项目概述

MazeBot是一个基于STM32的智能迷宫导航机器人系统，旨在实现小车在迷宫环境中的自动导航与寻路功能。项目采用上下位机架构，下位机负责硬件控制和数据采集，上位机负责SLAM建图、路径规划和可视化显示。

### 核心功能
- **实时SLAM建图**：基于RPLidar C1激光雷达的栅格地图构建
- **精准运动控制**：支持厘米级前进和度级转向的闭环控制
- **多传感器融合**：IMU948惯导模块 + 编码器里程计
- **实时可视化**：Python上位机提供实时点云显示和地图构建
- **蓝牙通信**：921600波特率高速数据传输

## 项目结构

```
MazeBot/
├── doc/                          # 文档目录
│   ├── Memo.md                  # 开发备忘录
│   └── 项目说明文档.md           # 本文档
├── HOST/                        # 上位机Python代码
│   ├── lidar_visualizer.py      # 主程序：雷达可视化和SLAM
│   ├── map_viewer.py            # 地图查看器
│   ├── requirements.txt         # Python依赖包
│   └── SYMBOLS_REFERENCE.md     # 符号体系参考手册
├── MazeBot.ino                  # 主程序入口
├── im948_CMD.ino                # IMU948驱动和里程计
├── im948_CMD.h                  # IMU948头文件
├── LidarModule.h                # RPLidar C1驱动
├── MotorControl.h               # 电机控制和PID算法
└── EncoderModule.h              # 编码器驱动
```

## 硬件架构

### 主控制器
- **STM32F103C8T6**：主控芯片，运行FreeRTOS实时操作系统
- **工作频率**：72MHz
- **内存**：64KB Flash + 20KB RAM

### 传感器系统
1. **RPLidar C1激光雷达**
   - 扫描频率：10Hz（100ms/圈）
   - 测距范围：0.1m - 12m
   - 角度分辨率：0.36°
   - 通信接口：UART（460800 baud）

2. **IMU948惯导模块**
   - 9轴传感器：加速度计+陀螺仪+磁力计
   - 数据输出：250Hz
   - 提供：欧拉角、位移、四元数等
   - 通信接口：UART（115200 baud）

3. **编码器系统**
   - 左右轮各一个增量式编码器
   - 分辨率：390脉冲/圈（13线×30减速比）
   - 接口：数字输入+中断

### 执行机构
- **差速驱动**：左右轮独立控制
- **电机**：直流减速电机
- **驱动**：H桥电机驱动板
- **轮径**：65mm
- **轮距**：100mm

### 通信模块
- **HC-04蓝牙模块**：921600 baud高速传输
- **数据包格式**：二进制协议，支持雷达+里程计融合

## 软件架构

### 下位机软件（Arduino/STM32）

#### 1. 主程序架构（MazeBot.ino）
```cpp
// FreeRTOS任务配置
- IMU900Task: 2ms周期，500Hz IMU数据更新
- LidarTask: 2ms周期，500Hz 雷达数据处理  
- CommandTask: 1ms周期，1kHz 蓝牙指令响应
- MotorControlTask: 5ms周期，200Hz PID控制
```

#### 2. 传感器驱动模块

**IMU948驱动（im948_CMD.ino）**
- 实现完整的IMU948通信协议
- 支持250Hz高频数据输出
- 提供环形缓冲区用于时间戳对齐
- 集成编码器里程计算法

**雷达驱动（LidarModule.h）**
- RPLidar C1标准协议实现
- 5字节数据包解析
- 质量过滤和距离范围限制
- 同步帧检测

**编码器驱动（EncoderModule.h）**
- 四倍频编码器解码
- 中断驱动的脉冲计数
- 线程安全的计数读取

#### 3. 运动控制系统（MotorControl.h）

**PID控制算法**
```cpp
// 三层PID控制结构
- 航向环PID：角度误差 → 目标速度差
- 速差环PID：速度差 → 占空比差分  
- 转向环PID：角度误差 → 转向占空比
```

**运动模式**
- **连续运动**：W/S/A/D键持续控制
- **精准运动**：F[cm]/B[cm] 厘米级前进后退
- **精准转向**：L[deg]/R[deg] 度级转向
- **闭环控制**：基于IMU和编码器的反馈控制

### 上位机软件（Python）

#### 1. 主程序（lidar_visualizer.py）

**核心功能模块**
- **数据接收**：多线程串口数据接收
- **SLAM算法**：简单栅格地图构建
- **实时可视化**：matplotlib动画显示
- **运动控制**：键盘和GUI控制界面

**技术特性**
- **多线程架构**：数据接收与可视化分离
- **实时性能**：6ms更新间隔，支持2000-4000包/秒
- **坐标系统一**：显示坐标系与地图坐标系一致
- **数据融合**：雷达+里程计时间戳对齐

#### 2. SLAM算法实现

**栅格地图参数**
```python
MAP_SIZE = 500          # 地图尺寸：500×500栅格
MAP_RESOLUTION = 0.05   # 分辨率：5cm/栅格
MAX_RANGE = 1.5         # 最大有效距离：1.5m
```

**建图算法**
- **Bresenham画线**：标记自由空间
- **概率累积**：障碍物置信度管理
- **旋转滤波**：防止旋转时建图拖尾
- **墙保护机制**：防止真实墙被误清除

#### 3. 可视化系统

**双窗口显示**
- **左窗口**：实时点云显示（极坐标→笛卡尔坐标）
- **右窗口**：SLAM栅格地图+机器人轨迹

**交互控制**
- **键盘控制**：方向键移动，空格急停
- **GUI按钮**：精准控制指令输入
- **实时调参**：距离校准、旋转滤波等

## 通信协议

### 数据包格式

#### 1. 雷达融合包（18字节）
```
包头(2) + 角度(4) + 距离(2) + 质量(1) + 里程计(8) + 校验(1)
0xAA55 + angle_q8 + dist_mm + quality + odom_data + checksum
```

#### 2. 独立里程计包（11字节）
```
包头(2) + X坐标(2) + Y坐标(2) + 角度(4) + 校验(1)  
0xBB55 + odom_x_cm + odom_y_cm + theta_q8 + checksum
```

#### 3. 同步标记
```
0xEE 0xEE  # 新的一圈扫描开始
```

### 控制指令协议

#### 1. 连续运动指令
- `W`：持续前进
- `S`：持续后退  
- `A`：持续左转
- `D`：持续右转
- `x`/`Space`：急停

#### 2. 精准运动指令
- `F[cm]`：前进N厘米（如F50=前进50cm）
- `B[cm]`：后退N厘米（如B30=后退30cm）
- `L[deg]`：左转N度（如L90=左转90°）
- `R[deg]`：右转N度（如R45=右转45°）

## 坐标系定义

### 显示坐标系（统一标准）
```
        Y↑ (前进方向)
         |
         |
         |
    ----+----> X (右侧方向)
         |
       原点
```

- **X轴**：水平向右为正 (m)
- **Y轴**：竖直向上为正 (m)  
- **θ (Heading)**：逆时针旋转为正，0°=X轴正方向 (度)
- **原点**：机器人启动位置 (地图中心 = 栅格坐标 250, 250)

### 坐标转换
```python
# IMU坐标系 → 显示坐标系
x_display = -y_imu
y_display = x_imu

# 世界坐标 → 栅格坐标  
x_grid = int(250 + (-y_world) / 0.05)
y_grid = int(250 + x_world / 0.05)
```

## 性能指标

### 实时性能
- **数据更新频率**：IMU 250Hz，雷达 10Hz
- **可视化更新**：6ms间隔（~167Hz）
- **控制响应**：1ms指令响应延迟
- **数据传输**：921600 baud，支持2000-4000包/秒

### 精度指标
- **位置精度**：±1cm/米（编码器里程计）
- **角度精度**：±1°（IMU航向角）
- **建图精度**：5cm栅格分辨率
- **控制精度**：厘米级前进，度级转向

## 技术特色

### 1. 多传感器融合
- **IMU里程计**：提供高精度航向角
- **编码器里程计**：提供精确位移测量
- **时间戳对齐**：雷达数据与里程计数据精确同步

### 2. 实时SLAM算法
- **旋转检测滤波**：防止旋转时建图产生拖尾
- **位姿插值校正**：根据扫描进度插值计算真实位姿
- **墙保护机制**：防止真实障碍物被误清除

### 3. 精准运动控制
- **三层PID控制**：航向环+速差环+转向环
- **闭环反馈**：基于IMU和编码器的实时反馈
- **多模式控制**：支持连续运动和精准运动

### 4. 高性能通信
- **多线程架构**：数据接收与处理分离
- **零延迟更新**：独立里程计通道
- **数据包优化**：二进制协议，高效传输

## 开发环境

### 下位机开发
- **IDE**：Arduino IDE 或 PlatformIO
- **框架**：Arduino Core for STM32
- **RTOS**：FreeRTOS
- **库依赖**：PID库

### 上位机开发
- **语言**：Python 3.8+
- **主要库**：
  - `pyserial`：串口通信
  - `numpy`：数值计算
  - `matplotlib`：可视化
  - `Pillow`：图像处理

### 硬件调试
- **串口调试**：USB转TTL模块
- **示波器**：信号质量检测
- **万用表**：电压电流测量

## 使用说明

### 1. 环境搭建
```bash
# 安装Python依赖
cd HOST
pip install -r requirements.txt
```

### 2. 硬件连接
- 确保所有传感器正确连接
- 检查电源电压（建议12V）
- 验证蓝牙模块配对

### 3. 程序运行
```bash
# 启动上位机
cd HOST
python lidar_visualizer.py

# 下位机通过Arduino IDE上传
```

### 4. 操作控制
- **实时控制**：方向键移动，空格急停
- **精准控制**：GUI按钮或指令输入
- **地图管理**：Ctrl+S保存，Ctrl+L查看

## 故障排除

### 常见问题
1. **蓝牙连接失败**：检查波特率设置（921600）
2. **雷达无数据**：检查串口连接和波特率（460800）
3. **IMU数据异常**：检查接线和校准状态
4. **运动不准确**：调整PID参数或编码器缩放系数

### 调试工具
- **串口监视器**：查看调试信息
- **数据诊断**：上位机提供数据包分析
- **参数调节**：实时调整距离校准等参数

## 项目进展

### 已完成功能 ✅
- [x] 硬件组装和传感器集成
- [x] 下位机FreeRTOS多任务架构
- [x] IMU948驱动和里程计算法
- [x] RPLidar C1驱动和数据解析
- [x] 电机PID控制和精准运动
- [x] 蓝牙高速通信协议
- [x] 上位机SLAM建图算法
- [x] 实时可视化和交互控制

### 待开发功能 📋
- [ ] 自动寻路算法集成
- [ ] 路径规划优化
- [ ] 地图保存和加载
- [ ] 多机器人协作
- [ ] 性能优化和稳定性提升

## 技术文档

### 相关文档
- `HOST/SYMBOLS_REFERENCE.md`：符号体系参考手册
- `doc/Memo.md`：开发备忘录和TODO列表
- `HOST/requirements.txt`：Python依赖包列表

### 代码注释
- 所有关键函数都有详细注释
- 重要参数都有说明和调优建议
- 包含调试开关和性能监控代码

## 总结

MazeBot项目成功实现了一个功能完整的迷宫导航机器人系统，具备以下特点：

1. **技术先进**：采用多传感器融合、实时SLAM、精准控制等先进技术
2. **性能优异**：高频率数据更新、低延迟控制响应、实时可视化
3. **架构清晰**：模块化设计、分层架构、易于扩展和维护
4. **实用性强**：支持多种控制模式、丰富的交互界面、完善的调试工具

该项目为后续的自动寻路算法开发奠定了坚实的技术基础，是一个优秀的机器人导航系统实现案例。

---

**文档版本**：V1.0  
**最后更新**：2025年1月14日  
**维护者**：MazeBot开发团队
