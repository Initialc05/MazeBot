# 编码器里程计升级说明
> **版本**: V4.1  
> **日期**: 2025-10-14  
> **目的**: 使用编码器替代IMU计算位移，解决旋转时漂移问题

---

## 🎯 升级内容

### **核心改进**：
- ✅ **X, Y位移**：从IMU加速度计积分 → 改为**编码器计算**
- ✅ **θ朝向**：保持使用**IMU陀螺仪**（原本就准确）
- ✅ **向后兼容**：可通过开关切换新旧版本

---

## 📊 预期效果

| 测试场景 | 纯IMU版本 | 编码器+IMU版本 |
|---------|-----------|----------------|
| **直线1米** | ±2cm | ±1cm ✅ |
| **原地转90°** | ±50cm漂移 ❌ | ±2cm ✅ |
| **原地转360°** | ±100cm漂移 ❌ | ±3cm ✅ |
| **长期累积误差** | 快速累积 | 缓慢累积 ✅ |

---

## 🔧 配置步骤

### **步骤1：测量小车参数**

#### **测量轮径（WHEEL_DIAMETER）**：
```
方法1：卷尺直接测量
  - 用卷尺量轮子外径（精确到1mm）
  - 例如：65mm = 0.065m

方法2：滚动测量（更准确）
  1. 在地上做标记
  2. 让轮子滚动10圈（保持直线）
  3. 测量总距离 L（米）
  4. 计算：WHEEL_DIAMETER = L / (10 × π)
  
  例如：滚动10圈，移动2.05米
       WHEEL_DIAMETER = 2.05 / (10 × 3.14159) = 0.0652m
```

#### **测量轮距（WHEEL_BASE）**：
```
方法：卷尺直接测量
  - 量两个轮子中心的距离（精确到2mm）
  - 例如：100mm = 0.100m
```

### **步骤2：修改配置文件**

打开 `im948_CMD.h` 文件，修改第13-19行：

```cpp
// 小车物理参数（需要实测后填入准确值）
#define WHEEL_DIAMETER 0.065   // ← 填入你测量的轮径（米）
#define WHEEL_BASE 0.100       // ← 填入你测量的轮距（米）

// 编码器里程计缩放系数（用于微调精度）
#define ENCODER_SCALE_FACTOR 1.0  // ← 调整缩放系数
```

**示例**：
```cpp
// 如果你测量结果是：轮径66mm，轮距102mm
#define WHEEL_DIAMETER 0.066   // 66mm = 0.066m
#define WHEEL_BASE 0.102       // 102mm = 0.102m
#define ENCODER_SCALE_FACTOR 1.05  // 如果显示距离比实际大5%，设为0.95
```

### **步骤3：编译上传**

1. 打开Arduino IDE
2. 编译代码（确保没有错误）
3. 上传到STM32
4. 等待上传完成

---

## 🧪 测试验证

### **测试1：直线精度**
```
1. 在地上做标记
2. 发送指令：F100（前进100cm）
3. 用卷尺测量实际距离
4. 误差应<2cm

如果误差较大，调整缩放系数：
- 显示距离 > 实际距离：减小 ENCODER_SCALE_FACTOR
- 显示距离 < 实际距离：增大 ENCODER_SCALE_FACTOR

例如：显示120cm，实际100cm → 设为 1.0 × (100/120) = 0.83
```

### **测试2：旋转漂移（关键测试）**
```
1. 在地上做标记，记录起始位置
2. 发送指令：R360（原地转一圈）
3. 观察小车是否回到原点
4. 用卷尺测量位移
5. 位移应<3cm ✅（纯IMU时>50cm ❌）
```

### **测试3：SLAM建图**
```
1. 运行上位机：python HOST/lidar_visualizer.py
2. 控制小车移动和旋转
3. 观察地图是否准确
4. 旋转时机器人位置应基本不动 ✅
```

---

## ⚙️ 开关控制

### **启用编码器里程计（默认）**：
```cpp
// im948_CMD.h 第10行
#define USE_ENCODER_ODOM true   // 新版：编码器+IMU
```

### **切换回纯IMU（测试对比）**：
```cpp
// im948_CMD.h 第10行
#define USE_ENCODER_ODOM false  // 旧版：纯IMU
```

修改后需要**重新编译上传**。

---

## 📁 已修改的文件

### **Arduino端**：
1. `im948_CMD.h` - 添加配置和声明
2. `im948_CMD.ino` - 实现编码器里程计
3. `LidarModule.h` - 修改雷达数据包来源

### **上位机**：
- `HOST/lidar_visualizer.py` - **无需修改** ✅
  - 数据包格式未变
  - 自动使用新的编码器数据

---

## 🔍 故障排查

### **Q1: 编译错误 "getLeftTicks undefined"**
```
检查：EncoderModule.h 是否被包含
解决：在MazeBot.ino中添加 #include "EncoderModule.h"
```

### **Q2: 小车移动1米，显示0.5米**
```
原因：轮径参数错误（偏小）
解决：重新测量轮径，修改WHEEL_DIAMETER
```

### **Q3: 旋转时仍然有漂移**
```
检查：
1. 确认 USE_ENCODER_ODOM = true
2. 确认重新编译上传了代码
3. 检查编码器是否正常工作（getLeftTicks()有数据）
```

### **Q4: 直线偏移**
```
原因：两轮编码器方向不一致
检查：EncoderModule.h 中编码器ISR的增减方向
```

---

## 📐 原理说明

### **编码器里程计算法**：

```
每个更新周期（250Hz）：

1. 读取编码器增量
   ΔL_ticks = leftTicks - lastLeftTicks
   ΔR_ticks = rightTicks - lastRightTicks

2. 转换为实际距离
   ΔL_dist = (ΔL_ticks / 390) × π × WHEEL_DIAMETER
   ΔR_dist = (ΔR_ticks / 390) × π × WHEEL_DIAMETER

3. 计算小车中心移动
   ΔS = (ΔL_dist + ΔR_dist) / 2

4. 使用IMU朝向（准确）
   θ = AngleZ（度）

5. 更新世界坐标
   X += ΔS × cos(θ)
   Y += ΔS × sin(θ)
```

### **为什么比IMU准确？**

```
IMU位移计算：
  加速度 → 积分 → 速度 → 再积分 → 位移
  问题：双重积分误差快速累积
        旋转时离心加速度误判为线性运动

编码器计算：
  脉冲数 → 轮子转角 → 移动距离
  优势：直接测量，无双重积分
        旋转时无离心力干扰
```

---

## 🚀 后续优化（可选）

如果需要更高精度，可以升级为：

### **卡尔曼滤波融合**：
```cpp
// 融合编码器和IMU加速度
X_fused = α × X_encoder + (1-α) × X_imu
// α = 0.98（编码器权重高）
```

### **打滑检测**：
```cpp
// 检测加速度和编码器是否一致
if (abs(accel_dist - encoder_dist) > threshold) {
    // 检测到打滑，降低编码器权重
}
```

---

## 📞 技术支持

如有问题，请检查：
1. 编码器是否正常工作（Serial输出leftTicks/rightTicks）
2. 轮径轮距参数是否准确测量
3. IMU是否正常提供AngleZ
4. 上位机Status显示的坐标是否合理

---

**最后更新**: 2025-10-14  
**维护者**: MazeBot开发团队

